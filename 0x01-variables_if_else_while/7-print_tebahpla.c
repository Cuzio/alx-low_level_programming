#include <stdio.h>

/**
 * main - mirror, let's give your something to look at.
 * Return: always 0
 */

int main(void)
{
	char c;

	for (c = 'z'; c >= 'a'; a--)
		putchar(c);

	putchar('\n');

	return (0);
}
