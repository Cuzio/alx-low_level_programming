#include <stdio.h>
/**
 * main - a program the prints exactly what was inputted.
 * Return: 0 (to show that it was successful)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
